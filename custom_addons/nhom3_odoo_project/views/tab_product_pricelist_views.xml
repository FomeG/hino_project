
    <odoo>
    
<!-- Kế thừa và chỉnh sửa form view gốc -->
    <record id="view_product_pricelist_form_inherit" model="ir.ui.view">
        <field name="name">product.pricelist.form.inherit</field>
        <field name="model">product.pricelist</field>
        <field name="inherit_id" ref="product.product_pricelist_view" />
        <field name="arch" type="xml">
            <!-- Thêm các trường mới vào tree view của item_ids -->
            <xpath expr="//field[@name='item_ids']/tree[1]" position="attributes">
                <attribute name="editable">bottom</attribute>
            </xpath>

            <!-- Chỉnh sửa các trường trong tree view của item_ids -->
            <xpath expr="//field[@name='item_ids']/tree[1]/field[@name='fixed_price']"
                position="before">
                <field name="x_discount_hmv" />
                <field name="x_discount_dealer" />
                <field name="price_discount" readonly="1" />
                <field name="x_discount_value" />
                <field name="x_auto" />
                <field name="x_allow_update_delete" />
            </xpath>

            <!-- Ẩn một số trường không cần thiết -->
            <xpath expr="//field[@name='item_ids']/tree[1]/field[@name='fixed_price']"
                position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <!-- Thêm các tab mới sau tab Configuration -->
            <xpath expr="//page[@name='pricelist_config']" position="after">
                <!-- Tab Áp dụng cho xe -->
                <page name="applyforvehicles" string="Apply for vehicles">
                    <group>
                        <group>
                            <field name="x_apply_for_vehicles" widget="radio" />
                            <field name="x_vehicle_ids"
                                widget="many2many_tags"
                                options="{'no_create': True}"
                                invisible="x_apply_for_vehicles != 'specific'"
                                required="x_apply_for_vehicles == 'specific'">
                                <tree>
                                    <field name="series_id" />
                                    <field name="model_list_id" />
                                    <field name="vin" />
                                    <field name="engine_number" />
                                </tree>
                            </field>
                        </group>
                    </group>
                </page>

                <!-- Tab Áp dụng cho khách hàng -->
                <page name="applyforcustomer" string="Apply for customers">
                    <group>
                        <group>
                            <field name="x_apply_for" widget="radio" />
                            <field name="x_customer_ids"
                                widget="many2many_tags"
                                options="{'no_create': True}"
                                invisible="x_apply_for != 'specific'"
                                required="x_apply_for == 'specific'" />
                        </group>
                    </group>
                </page>

                <!-- Tab Áp dụng theo KM -->
                <page name="applyforkm" string="Apply for kilometers">
                    <group>
                        <group>
                            <field name="x_from_km" />
                            <field name="x_to_km" />
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>


      <record id="view_tab_apdung_pricelist" model="ir.ui.view">
        <field name="name">view.tab.vehicles.pricelist</field>
        <field name="model">product.pricelist</field>
        <field name="inherit_id" ref="product.product_pricelist_view" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='pricelist_config']" position="after">
                <page name="applyforvehicles" string="Apply for vehicles">
                    <group>
                        <group>
                            <field name="x_apply_for_vehicles" widget="radio"/>
                            <field name="x_vehicle_ids" 
                                widget="many2many_tags"
                                options="{'no_create': True}"
                                invisible="x_apply_for_vehicles != 'specific'"
                                required="x_apply_for_vehicles == 'specific'">
                                <tree>
                                    <field name="series_id"/>
                                    <field name="model_list_id"/>
                                    <field name="vin"/>
                                    <field name="engine_number"/>
                                </tree>
                            </field>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

        <record id="view_tab_apdungkm_pricelist" model="ir.ui.view">
        <field name="name">view.tab.apdungkm.pricelist</field>
        <field name="model">product.pricelist</field>
        <field name="inherit_id" ref="product.product_pricelist_view" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='pricelist_config']" position="after">
                <page name="applykm" string="Apply KM">
                    <group>
                        <group>
                            <field name="x_from_km"/>
                            <field name="x_to_km"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

        <record id="view_tab_apdungdaily_pricelist" model="ir.ui.view">
        <field name="name">view.tab.dealers.pricelist</field>
        <field name="model">product.pricelist</field>
        <field name="inherit_id" ref="product.product_pricelist_view" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='pricelist_config']" position="after">
                <page name="applyformerchants" string="Apply for dealers">
                    <group>
                        <group>
                            <field name="x_apply_for_dealer" widget="radio"/>
                            <field name="x_dealer_ids" 
                                widget="many2many_tags"
                                options="{'no_create': True}"
                                invisible="x_apply_for_dealer != 'specific'"
                                required="x_apply_for_dealer == 'specific'">
                                <tree>
                                    <field name="name"/>
                                    <field name="company_registry"/>  <!-- Company code -->
                                </tree>
                            </field>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

       <record id="view_tab_khachhang_pricelist" model="ir.ui.view">
        <field name="name">view.tab.khachhang.pricelist</field>
        <field name="model">product.pricelist</field>
        <field name="inherit_id" ref="product.product_pricelist_view" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='pricelist_config']" position="after">
                <page name="applyforcustomer" string="Apply for customers">
                    <group>
                        <group>
                            <field name="x_apply_for" widget="radio" />
                            <field name="x_customer_ids"
                                widget="many2many_tags"
                                options="{'no_create': True}"
                                invisible="x_apply_for != 'specific'"
                                required="x_apply_for == 'specific'" />
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    
    </odoo>
